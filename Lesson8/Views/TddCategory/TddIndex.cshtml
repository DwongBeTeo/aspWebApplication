@model IEnumerable<Lesson8.Models.TddCategory>

@{
    ViewBag.Title = "TddIndex";
}

<h2>TddIndex</h2>

<p>
    @Html.ActionLink("Tdd:Create New", "TddCreate")
</p>
<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TddCategoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TddCategoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TddCategoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TddCategoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TddCategoryId })
        </td>
    </tr>
}

</table>
